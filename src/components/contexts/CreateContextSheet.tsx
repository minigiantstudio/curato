'use client'
export function CreateContextSheet(_: {
  open: boolean
  defaultType: 'brand' | 'project'
  brands: import('@/types/context').Context[]
  onClose: () => void
  onCreated: () => void
}) {
  return null
}
